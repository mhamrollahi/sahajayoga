<div class="row">

  <div
    class="d-flex align-self-center justify-content-center order-2 order-lg-1 col-lg-5 col-12"
  >
    <div class="login-register-form-wrap">

      <div class="content">
        <h1 style="font-weight:500;font-size:38px">ثبت نام</h1>
        <p>در این بخش برای استفاده بیشتر از امکانات سایت می توانید ثبت نام کنید.</p>
      </div>
       {{#if hasError}}
          <div class="alert alert-danger">
            {{#each errors}}
              <small class="errMessage">{{this}}</small>
            {{/each}}
          </div>
        {{/if}}
       {{#if success}}
          <div class="alert alert-success">
            <small class="successMessage">{{success}}</small>
          </div>
        {{/if}}

      <div class="login-register-form">
        <form action="./register" method="POST" data-validation >
          <div class="row">

            <div class="col-12 mb-20"><input name="email" type="email"
                class="form-control"
                placeholder="پست الکترونیکی" autofocus="true" required data-farsiname="پست الکترونیکی"
              /></div>
            <div class="col-12 mb-20"><input
                class="form-control" name="password"
                type="password"
                placeholder="کلمه عبور" required minlength="4"
              /></div>
            <div class="col-12 mb-20"><input
                class="form-control" name="password_confirmation"
                type="password"
                placeholder="تایید کلمه عبور" required minlength="4"
              /></div>
            <div class="col-12">
              <div class="row justify-content-between">
                <div class="col-auto mb-15">در حال حاضر حساب کاربری دارید؟
                  <a href="./login">هم اکنون وارد شوید.</a></div>
              </div>
            </div>
            <div class="col-12 mt-10"><button
                class="button button-primary button-outline"
              >ثبت نام</button></div>
          </div>
        </form>

      </div>
    </div>
  </div>

  <div class="login-register-bg order-1 order-lg-2 col-lg-7 col-12">
    <div class="content">
      <h1 >ثبت نام</h1>
        <p>در این بخش برای استفاده بیشتر از امکانات سایت می توانید ثبت نام کنید.</p>
    </div>
  </div>

</div>

<script src="../../assets/js/frmValidation.js"></script>